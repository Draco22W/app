server {
    listen 80;
    server_name 101.35.103.216;  # 你的服务器公网IP或域名

    root D:/app/projects/games/whack-a-mole/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # 如果有后端API，可以这样转发
    # location /api/ {
    #     proxy_pass http://localhost:3000/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    # }
}
